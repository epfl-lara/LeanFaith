# SFT1 agent kickoff: deliver data first

You own SFT1 delivery. Implement and publish the dataset, then keep the authorized library
generation moving. The user explicitly wants SFT1 before other new work and wants to stop the
cycle of repeated reviews and failed pilots.

Read `PLAN.md`, all of `plans/00_shared_contracts.md`, and exactly the SFT1 task brief,
`plans/30_sft1_deterministic.md`. Then read
`plans/reviews/2026-09-05-production/execution/SFT1_DATA_FIRST_PLAN.md` as the execution contract.
It incorporates GPT Pro and Fable 5.1, correcting their disputed crash diagnosis and dataset counts.
Use the review branch `milikic/sft1-production-review-20260905`, which includes SFT1 production
`7498fbf4e78421012c1c729d05576c345c5f16f1`, or a verified descendant. Create an isolated
`milikic/sft1-data-first-delivery` worktree, claim SFT1 in its brief, and preserve unrelated work.

Your first milestone is a new private publication in `Lemmy00/leanfaith-sft1-deterministic-v1`
with a directly usable sampling view and a verified Hub revision. Do not stop at a plan, patch,
local pool, or passing tests. Use existing authorization for the catalog and incremental private
publication; no repeat catalog review is needed for the mechanisms named in the execution plan.

First, repair release assembly using zero Lean. Recover the completed Wave 4 base and supplement
through original configs/receipts. Select complete groups deterministically before strict
materialization, preserve occurrence provenance, drop competing same-label groups whole, and
fail closed on conflicting labels or corrupt identities. Expected pre-view recovery is
319 groups / 1,161 physical rows / 202 roots. Keep an exact-200 inspection view and finish the
existing rendered-pair inspection once; retain the extra roots in the pool.

Create an additive certified-curriculum policy in which physical feature-cell balance is a
diagnostic, not certificate admission. Update the selector, integrity/release checks, and consumed
view schema together; preserve frozen policies and results. Do not change the Lean engine to
recover these rows. Keep `{reference, candidate, label}` as the model fields. Put provenance,
group membership, and sampling weights in keyed sidecars.

For composition, distribute root-balanced mass among complete groups, give each of the four
logical roles `q_g/4`, and sum contributions on shared physical edges. Verify effective 50/50
labels and one-sided endpoint marginals. Keep N25 <=25% of physical released rows AND exposure;
N19 stays excluded. Record pairwise shortcuts and all failed screens honestly. Publish the pool,
default weighted view, manifests, and load/sample example under an additive prefix. Recover
eligible Wave 3 singles as a separate explicit view afterward if they would delay this first upload.

Then produce additional SFT1 immediately: census already certified N31/N32/N26 negatives and
generate only missing square closures with the existing square operations. Join caches and the
historical union before scheduling work. Follow with a resumable Mathlib single-hop sweep using
the full existing operation IDs in the plan, an additive config, explicit inputs, and new run IDs.
Measure a fixed 1,000-root prefix only where the current workload is unmeasured, preserve its
outputs, and continue the same queue through 10K and the remaining inventory when useful net
yield and runtime support it. Publish completed useful shards incrementally. Prioritize newly
released non-N25 pairs per Lean-second, not attempted roots or raw census size.

Lean is the bottleneck: do cheap filtering/joins/dedup first, reuse persistent workers/caches,
and never recompile a completed corpus for confidence. Use one claimed Lean worker by default
within the shared two-worker/40-GiB cap. Long jobs require pinned committed code/configs, an
explicit controller deadline, named detached tmux, append-only journals, resume commands, and
verified process/journal advancement. The old `--sprint-end-utc` option alone does not bound a run.

Run only the focused regressions for changed release/sampling behavior, one end-to-end
serialization/publish path, and resume/dedup checks; reuse prior unchanged-engine evidence.
Do not restart compiler audits, optimize composition, add mechanisms, train models, or work on
SFT2/CPT/baselines before the SFT1 delivery. Ordinary low yield is terminal accounting and queue
prioritization, not a reason to restart a review program. Real label/certificate defects remain
blocking and must be reported precisely.

Report progress in delivered rows: published revision, physical rows, roots/groups, net novelty,
family distribution, weighted class/N25 mass, limitations, and newly generated rows per hour.
The frozen releases overlap: their union is 18,474, not 20,396. The observed 19,671 total including
Wave 3/4 is only an input union, not a validated cap-compliant release. Do not promise 35–40K or
100K until measured. Your first response should state the immediate implementation action and
then execute it; another general assessment is not the deliverable.
