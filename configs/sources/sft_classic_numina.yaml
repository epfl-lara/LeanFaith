# Source config: sft_classic_numina (PLAN.md §9.1, §9.3, B.2).
# Public sibling of the requested private source; first entry in the fixed
# NL-source fallback order. Schema and counts below are expectations to be
# rechecked at the pinned revision, not verified facts.
source_config_version: 1
source: sft_classic_numina
kind: huggingface_dataset
dataset_id: formalmathatepfl/sft_classic_numina
expected_columns: [uuid, question, answer, lean_code]
expected_scale_rows: 99774  # approximate; verify pinned schema and exact counts at probe (§9.1)
adapter_mapping:  # verified mapping per §9.3, rechecked at the pinned revision
  problem_id: uuid
  nl_statement: question
  solution_text: answer
  reference_or_candidate_lean: lean_code
lean_code_trust: recorded_by_adapter  # the adapter records trusted|generated|unknown; trust is never inferred from the column name (§9.3)
role: public_fallback_1
